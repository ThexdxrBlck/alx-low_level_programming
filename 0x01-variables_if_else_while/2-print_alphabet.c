#include <stdio.h>

/**
 * main - prints alphabet in lower case format
 * Return: Always 0
 */
int main(void)
{
	char lc;

	for (lc = 'a'; la <= 'z'; lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
