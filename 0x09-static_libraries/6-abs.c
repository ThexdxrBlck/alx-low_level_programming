#include "main.h"

/**
 * _abs - computes the absolue value of an integer
 * @n: the int to check
 * Return: the absolue value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
